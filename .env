# ClarifyAll Frontend Standalone Configuration
REACT_APP_NAME=ClarifyAll
REACT_APP_VERSION=1.0.0
REACT_APP_DESCRIPTION=Discover and explore the best tools for your needs

# Since this is a standalone version, no API URLs are needed
# All data is handled locally through JSON files